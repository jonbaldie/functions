version: 2.1

jobs:
  build:
    docker:
      - image: thecodingmachine/php:8.1-v4-cli
    steps:
      - run: git clone https://github.com/jonbaldie/functions.git && cd functions

workflows:
  build_and_test:
    jobs:
      - build
